<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= contactPage?.heroTitle || 'İletişim' %> | <%= siteInfo?.siteName || 'Selim Sıhhi Tesisat' %> - 7/24 Acil Servis</title>
    <meta name="description" content="<%= contactPage?.heroDescription || 'Selim Sıhhi Tesisat ile iletişime geçin. İzmir genelinde 7/24 acil tesisat hizmeti. Hızlı çözüm, uygun fiyat.' %>">
    <meta name="keywords" content="tesisat iletişim, acil tesisat servisi, istanbul tesisatçı iletişim, 7/24 tesisat hizmeti">
    <meta name="robots" content="index, follow">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="İletişim | Selim Sıhhi Tesisat - 7/24 Acil Servis">
    <meta property="og:description" content="Selim Sıhhi Tesisat ile iletişime geçin. İzmir genelinde 7/24 acil tesisat hizmeti.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://selimtesisat.com/contact">
    <meta property="og:image" content="https://selimtesisat.com/images/contact-og.jpg">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- CSS -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
        }

        .header {
            background: linear-gradient(135deg, #0EA5E9 0%, #4C1D95 100%);
            color: white;
            padding: 1rem 0;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .contact-logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.8rem;
            font-weight: 700;
        }

        .contact-logo img {
            height: 120px;
            width: auto;
            border-radius: 10px;
            background: #fff;
            border: 2px solid #0EA5E9;
            box-shadow: 0 2px 10px 0 rgba(14,165,233,0.10);
            padding: 4px 10px;
            transition: box-shadow 0.2s, background 0.2s;
        }

        .contact-logo img:hover {
            background: #e0f2fe;
            box-shadow: 0 6px 18px 0 rgba(14,165,233,0.18);
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-menu a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            padding: 0.5rem 1rem;
            border-radius: 8px;
        }

        .nav-menu a:hover {
            background: rgba(255,255,255,0.1);
            transform: translateY(-2px);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .hero-section {
            text-align: center;
            padding: 3rem 0;
            background: white;
            border-radius: 20px;
            margin-bottom: 3rem;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
        }

        .hero-section h1 {
            font-size: 3rem;
            color: #4C1D95;
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .hero-section p {
            font-size: 1.2rem;
            color: #64748b;
            max-width: 600px;
            margin: 0 auto;
        }

        .contact-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .contact-info {
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
        }

        .contact-info h2 {
            color: #4C1D95;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
            padding: 1rem;
            border-radius: 12px;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            transition: all 0.3s ease;
        }

        .contact-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .contact-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #0EA5E9 0%, #4C1D95 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
        }

        .contact-form {
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: #4C1D95;
            font-weight: 600;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8fafc;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #0EA5E9;
            background: white;
            box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .urgent-checkbox {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
            padding: 1rem;
            background: #fef2f2;
            border-radius: 12px;
            border: 2px solid #fecaca;
        }

        .urgent-checkbox input[type="checkbox"] {
            width: auto;
            margin: 0;
        }

        .urgent-checkbox label {
            color: #dc2626;
            font-weight: 600;
            margin: 0;
        }

        .submit-btn {
            width: 100%;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, #0EA5E9 0%, #4C1D95 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(14, 165, 233, 0.3);
        }

        .emergency-banner {
            background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
            color: white;
            text-align: center;
            padding: 1.5rem;
            border-radius: 12px;
            margin-bottom: 2rem;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        .emergency-banner h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .emergency-banner a {
            color: white;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.2rem;
        }

        .service-areas {
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
            margin-bottom: 2rem;
        }

        .service-areas h2 {
            color: #4C1D95;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .areas-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
        }

        .area-item {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            padding: 1rem;
            border-radius: 12px;
            text-align: center;
            transition: all 0.3s ease;
        }

        .area-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .footer {
            background: #4C1D95;
            color: white;
            text-align: center;
            padding: 2rem;
            margin-top: 3rem;
        }

        .alert {
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1rem;
            display: none;
        }

        .alert.success {
            background: #d1fae5;
            color: #065f46;
            border: 1px solid #a7f3d0;
        }

        .alert.error {
            background: #fee2e2;
            color: #991b1b;
            border: 1px solid #fecaca;
        }

        .mobile-menu {
            display: none;
        }

        @media (max-width: 768px) {
            .nav-menu {
                display: none;
            }

            .mobile-menu {
                display: block;
                background: none;
                border: none;
                color: white;
                font-size: 1.5rem;
                cursor: pointer;
            }

            .hero-section h1 {
                font-size: 2rem;
            }

            .contact-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .container {
                padding: 1rem;
            }

            .areas-grid {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="nav-container">
            <a href="/" class="contact-logo">
                <img src="<%= siteInfo?.logo || '/images/selim-logo.png' %>" alt="<%= siteInfo?.siteName || 'Selim Tesisat' %> Logo">
            </a>
            <nav>
                <ul class="nav-menu">
                    <li><a href="/">Ana Sayfa</a></li>
                    <li><a href="/hizmetler">Hizmetler</a></li>
                    <li><a href="/hakkimizda">Hakkımızda</a></li>
                    <li><a href="/referanslar">Referanslar</a></li>
                    <li><a href="/galeri">Galeri</a></li>
                    <li><a href="/iletisim">İletişim</a></li>
                </ul>
                <button class="mobile-menu">☰</button>
            </nav>
        </div>
    </header>

    <main class="container">
        <section class="hero-section">
            <img src="<%= contactPage?.heroImage || '/images/stockvault-pipes-with-handle97235.jpg' %>" alt="<%= contactPage?.heroTitle || 'İletişim' %> <%= siteInfo?.siteName || 'Selim Tesisat' %>" style="max-width: 320px; width: 100%; border-radius: 18px; margin-bottom: 2rem; box-shadow: 0 8px 32px rgba(14,165,233,0.10);">
            <h1><%= contactPage?.heroTitle || 'İletişim' %></h1>
            <p><%= contactPage?.heroDescription || '7/24 acil servis ile hizmetinizdeyiz. İzmir genelinde hızlı ve güvenilir tesisat hizmeti.' %></p>
        </section>

        <div class="emergency-banner">
            <h3><%= contactPage?.emergencyTitle || '🚨 ACİL DURUM 🚨' %></h3>
            <p><%= contactPage?.emergencyDescription || 'Acil tesisat ihtiyacınız için hemen arayın:' %> <a href="tel:<%= contactInfo?.emergencyPhone || '+905551234567' %>"><%= contactInfo?.phone || '+90 555 123 45 67' %></a></p>
        </div>

        <div class="contact-grid">
            <div class="contact-info">
                <h2><%= contactPage?.contactInfoTitle || 'İletişim Bilgileri' %></h2>
                <img src="<%= contactPage?.contactInfoImage || '/images/nikkan-navidi-Y9xZc0PSTvo-unsplash.jpg' %>" alt="İletişim Ofisi" style="width:100%;height:120px;object-fit:cover;border-radius:10px;margin-bottom:1rem;">
                
                <div class="contact-item">
                    <div class="contact-icon">📞</div>
                    <div>
                        <strong>Telefon</strong><br>
                        <a href="tel:<%= contactInfo?.phone || '+905551234567' %>" style="color: #0EA5E9; text-decoration: none;"><%= contactInfo?.phone || '+90 555 123 45 67' %></a>
                    </div>
                </div>

                <div class="contact-item">
                    <div class="contact-icon">📧</div>
                    <div>
                        <strong>E-posta</strong><br>
                        <a href="mailto:<%= contactInfo?.email || 'info@selimtesisat.com' %>" style="color: #0EA5E9; text-decoration: none;"><%= contactInfo?.email || 'info@selimtesisat.com' %></a>
                    </div>
                </div>

                <div class="contact-item">
                    <div class="contact-icon">📍</div>
                    <div>
                        <strong>Hizmet Alanı</strong><br>
                        <%= contactInfo?.address || 'İzmir - Tüm İlçeler' %>
                    </div>
                </div>

                <div class="contact-item">
                    <div class="contact-icon">🕐</div>
                    <div>
                        <strong>Çalışma Saatleri</strong><br>
                        <%= contactInfo?.workingHours || '7/24 Acil Servis' %>
                    </div>
                </div>

                <div class="contact-item">
                    <div class="contact-icon">⚡</div>
                    <div>
                        <strong>Yanıt Süresi</strong><br>
                        <%= contactPage?.responseTime || '30 Dakika içinde' %>
                    </div>
                </div>
            </div>

                        <div class="contact-form">
                <h2><%= contactPage?.formTitle || 'Mesaj Gönder' %></h2>
                <p style="color: #64748b; margin-bottom: 2rem;"><%= contactPage?.formDescription || 'Tesisat sorunlarınız için hemen bizimle iletişime geçin.' %></p>
                
                <form id="contactForm" action="/contact" method="POST" style="display: flex; flex-direction: column; gap: 1rem;">
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                        <div>
                            <label for="firstName" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.firstNameLabel || 'Ad' %></label>
                            <input type="text" id="firstName" name="firstName" required style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                        </div>
                        <div>
                            <label for="lastName" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.lastNameLabel || 'Soyad' %></label>
                            <input type="text" id="lastName" name="lastName" required style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                        </div>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                        <div>
                            <label for="phone" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.phoneLabel || 'Telefon' %></label>
                            <input type="tel" id="phone" name="phone" required style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                        </div>
                        <div>
                            <label for="email" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.emailLabel || 'E-posta' %></label>
                            <input type="email" id="email" name="email" required style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                        </div>
                    </div>
                    
                    <div>
                        <label for="service" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.serviceLabel || 'Hizmet Türü' %></label>
                        <select id="service" name="service" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                            <option value=""><%= contactPage?.serviceSelectDefault || 'Hizmet seçin...' %></option>
                            <% services?.forEach(service => { %>
                                <option value="<%= service.name %>"><%= service.name %></option>
                            <% }) %>
                        </select>
                    </div>
                    
                    <div>
                        <label for="location" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.locationLabel || 'Konum' %></label>
                        <input type="text" id="location" name="location" required placeholder="<%= contactPage?.locationPlaceholder || 'Bornova, İzmir' %>" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px;">
                    </div>
                    
                    <div>
                        <label for="message" style="display: block; margin-bottom: 0.5rem; font-weight: 500;"><%= contactPage?.messageLabel || 'Mesajınız' %></label>
                        <textarea id="message" name="message" required rows="5" placeholder="<%= contactPage?.messagePlaceholder || 'Sorunuzu detaylı bir şekilde açıklayın...' %>" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px; resize: vertical;"></textarea>
                    </div>
                    
                    <button type="submit" style="background: #0EA5E9; color: white; border: none; padding: 1rem 2rem; border-radius: 8px; font-weight: 600; cursor: pointer; transition: background-color 0.2s;">
                        <%= contactPage?.submitButton || 'Mesaj Gönder' %>
                    </button>
                </form>
            </div>
        </div>

        <section class="service-areas">
            <h2><%= contactPage?.serviceAreasTitle || 'Hizmet Verdiğimiz Bölgeler' %></h2>
            <img src="<%= contactPage?.serviceAreasImage || '/images/taylor-heery-M0lMY68aMfM-unsplash.jpg' %>" alt="Hizmet Bölgeleri" style="width:100%;max-width:600px;display:block;margin:0 auto 2rem auto;border-radius:12px;box-shadow:0 2px 10px rgba(14,165,233,0.08);">
            <div class="areas-grid">
                <% (servicesPage?.serviceAreas || contactPage?.serviceAreas || [
                    'Bornova', 'Konak', 'Buca', 'Karşıyaka', 'Balçova', 'Çiğli',
                    'Gaziemir', 'Karabağlar', 'Narlıdere', 'Bayraklı', 'Alsancak', 'Üçyol'
                ]).forEach(area => { %>
                    <div class="area-item"><%= area %></div>
                <% }) %>
            </div>
        </section>
    </main>

    <footer class="footer">
        <p>&copy; 2025 <%= siteInfo?.siteName || 'Selim Sıhhi Tesisat ve Tamirat' %>. Tüm hakları saklıdır.</p>
        <p><%= siteInfo?.footerSlogan || 'Kalite ile Tanışın... 💧' %></p>
    </footer>

    <script>
        // Form submission handler
        document.getElementById('contactForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const data = {
                name: formData.get('name'),
                email: formData.get('email'),
                phone: formData.get('phone'),
                service: formData.get('service'),
                message: formData.get('message'),
                urgent: formData.get('urgent') === 'on'
            };

            const alertDiv = document.getElementById('alert');
            const submitBtn = this.querySelector('.submit-btn');
            
            // Show loading state
            submitBtn.textContent = 'Gönderiliyor...';
            submitBtn.disabled = true;

            try {
                const response = await fetch('/contact/submit', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(data)
                });

                const result = await response.json();

                if (result.success) {
                    alertDiv.className = 'alert success';
                    alertDiv.textContent = result.message;
                    alertDiv.style.display = 'block';
                    this.reset();
                } else {
                    alertDiv.className = 'alert error';
                    alertDiv.textContent = result.message || 'Bir hata oluştu.';
                    alertDiv.style.display = 'block';
                }
            } catch (error) {
                alertDiv.className = 'alert error';
                alertDiv.textContent = 'Mesaj gönderilirken bir hata oluştu. Lütfen tekrar deneyiniz.';
                alertDiv.style.display = 'block';
            } finally {
                submitBtn.textContent = 'Mesaj Gönder';
                submitBtn.disabled = false;
            }
        });

        // Phone number formatting
        document.getElementById('phone').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length > 10) {
                value = value.substring(0, 10);
            }
            e.target.value = value;
        });

        // Urgent checkbox handler
        document.getElementById('urgent').addEventListener('change', function(e) {
            const submitBtn = document.querySelector('.submit-btn');
            if (e.target.checked) {
                submitBtn.style.background = 'linear-gradient(135deg, #dc2626 0%, #991b1b 100%)';
                submitBtn.textContent = '🚨 ACİL MESAJ GÖNDER';
            } else {
                submitBtn.style.background = 'linear-gradient(135deg, #0EA5E9 0%, #4C1D95 100%)';
                submitBtn.textContent = 'MESAJ GÖNDER';
            }
        });

        // Mobile menu toggle
        document.querySelector('.mobile-menu').addEventListener('click', function() {
            const navMenu = document.querySelector('.nav-menu');
            navMenu.style.display = navMenu.style.display === 'flex' ? 'none' : 'flex';
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>

    <!-- Structured Data -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "LocalBusiness",
        "name": "Selim Sıhhi Tesisat ve Tamirat",
        "description": "İzmir'da profesyonel sıhhi tesisat hizmetleri",
        "telephone": "+90 555 123 45 67",
        "email": "info@selimtesisat.com",
        "address": {
            "@type": "PostalAddress",
            "addressLocality": "İzmir",
            "addressCountry": "TR"
        },
        "openingHours": "Mo-Su 00:00-23:59",
        "serviceArea": {
            "@type": "City",
            "name": "İzmir"
        },
        "priceRange": "$$",
        "hasOfferCatalog": {
            "@type": "OfferCatalog",
            "name": "Tesisat Hizmetleri",
            "itemListElement": [
                {
                    "@type": "Offer",
                    "itemOffered": {
                        "@type": "Service",
                        "name": "Su Kaçağı Tamiri"
                    }
                },
                {
                    "@type": "Offer",
                    "itemOffered": {
                        "@type": "Service",
                        "name": "Tıkanıklık Açma"
                    }
                },
                {
                    "@type": "Offer",
                    "itemOffered": {
                        "@type": "Service",
                        "name": "Musluk Tamiri"
                    }
                }
            ]
        }
    }
    </script>
</body>
</html>